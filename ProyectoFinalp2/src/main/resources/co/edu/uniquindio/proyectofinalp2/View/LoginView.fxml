<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<AnchorPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="co.edu.uniquindio.proyectofinalp2.ViewController.LoginController"
            prefWidth="500" prefHeight="600">

    <VBox alignment="CENTER" spacing="0"
          AnchorPane.topAnchor="50" AnchorPane.bottomAnchor="50"
          AnchorPane.leftAnchor="50" AnchorPane.rightAnchor="50">

        <VBox alignment="CENTER" spacing="25">
            <padding>
                <Insets top="40" right="40" bottom="40" left="40" />
            </padding>

            <Label text="📦">
                <VBox.margin>
                    <Insets bottom="10" />
                </VBox.margin>
            </Label>

            <VBox alignment="CENTER" spacing="5">
                <Label text="Sistema de Envíos" />
                <Label text="Inicia sesión para continuar" />
            </VBox>

            <VBox spacing="15" prefWidth="320">
                <VBox spacing="5">
                    <Label text="Correo Electrónico" />
                    <TextField fx:id="txtCorreo" promptText="correo@ejemplo.com" />
                </VBox>

                <VBox spacing="5">
                    <Label text="Contraseña" />
                    <PasswordField fx:id="txtContrasena" promptText="••••••••" />
                </VBox>
            </VBox>

            <VBox spacing="10" alignment="CENTER">
                <Label text="Tipo de Usuario" />
                <HBox alignment="CENTER" spacing="20">
                    <RadioButton fx:id="rdbUsuario" text="👤 Usuario" selected="true" />
                    <RadioButton fx:id="rdbAdministrador" text="⚙️ Administrador" />
                    <RadioButton fx:id="rdbRepartidor" text="🚚 Repartidor" />
                </HBox>
            </VBox>

            <VBox spacing="12" prefWidth="320">
                <padding>
                    <Insets top="10" />
                </padding>
                <Button text="Iniciar Sesión" onAction="#handleLogin"
                        prefWidth="320" prefHeight="45" />

                <Button text="Crear Cuenta Nueva" onAction="#handleRegistro"
                        prefWidth="320" prefHeight="40" />
            </VBox>

            <Label text="¿Olvidaste tu contraseña?">
                <VBox.margin>
                    <Insets top="10" />
                </VBox.margin>
            </Label>
        </VBox>
    </VBox>
</AnchorPane>